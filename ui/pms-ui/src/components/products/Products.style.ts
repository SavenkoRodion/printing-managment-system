import { SxProps } from "@mui/material";

const borderNone: SxProps = { border: "none" };

const tablePadding: SxProps = { paddingTop: "16px" };

const buttonMargin: SxProps = { marginRight: "5px" };

const styles = {
  borderNone,
  tablePadding,
  buttonMargin,
};

export default styles;
